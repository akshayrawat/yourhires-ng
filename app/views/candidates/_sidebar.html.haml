-content_for :sidebar do
	.sidebar-section
		%center
			%p
				=link_to "New Feedback", "#{new_candidate_feedback_url(@candidate)}", :class => "bold-button"
			%p
				=link_to "Schedule a Step", "#{candidate_schedule_step_url(@candidate)}", :class => "bold-button"
