%span.title-links
	=link_to("Profile", candidate_url(@candidate), :id => "candidates-show-navigation")
	=link_to("Schedule", candidate_schedule_url(@candidate), :id => "candidates-schedule-navigation")
	=link_to("Feedbacks", candidate_feedbacks_url(@candidate), :id => "feedbacks-index-navigation")
	
:javascript
	$("##{controller_name}-#{action_name}-navigation").addClass('selected');