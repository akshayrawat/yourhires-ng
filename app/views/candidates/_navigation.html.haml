%span.title-links
	=link_to("Profile", candidate_url(@candidate), :id => "show-candidate-navigation")
	=link_to("Schedule", candidate_schedule_url(@candidate), :id => "schedule-candidate-navigation")
	=link_to("Feedbacks", candidate_feedbacks_url(@candidate), :id => "feedbacks-candidate-navigation")
	
:javascript
	$("##{action_name}-candidate-navigation").addClass('selected');